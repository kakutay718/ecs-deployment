# Whizlabs UI (minimal scaffold)

This is a minimal Vite + React + Tailwind scaffold containing the provided `Home` component.

Quick start:

```bash
npm install
npm run dev
```

Notes:
- Tailwind and PostCSS configs are included.
- If you prefer pnpm or yarn, use those commands instead of npm.
